export default function SupplierMaterialsTable({ materials = [] }) { if (!materials.length) return <p className="border-y border-[var(--line)] py-10 text-center font-sans text-sm text-[var(--muted)]">No material history found.</p>; return <div className="overflow-x-auto border-y border-[var(--line)]"><table className="w-full min-w-[560px] text-left font-sans text-sm"><thead className="text-[10px] uppercase tracking-wider text-[var(--muted)]"><tr><th className="py-3">Material</th><th>Quantity</th><th>Status</th><th className="text-right">Remaining</th></tr></thead><tbody className="divide-y divide-[var(--line)]">{materials.map(row => <tr key={row.id}><td className="py-4 font-bold">{row.name}</td><td>{row.quantity}</td><td><span className={row.status === 'paid' ? 'text-[var(--teal)]' : 'text-[var(--coral)]'}>{row.status === 'as_dept' ? 'On debt' : 'Paid'}</span></td><td className="text-right">${Number(row.remaining_amount || 0).toLocaleString()}</td></tr>)}</tbody></table></div>; }
