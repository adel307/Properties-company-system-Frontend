import Link from 'next/link';
export default function EntityPage({ title, eyebrow, description, rows = [], columns = [], action = 'Add record' }) { return <div className="fade-up"><div className="flex flex-wrap items-end justify-between gap-5 border-b border-[var(--line)] pb-8"><div><p className="font-sans text-[10px] font-bold uppercase tracking-[.2em] text-[var(--teal)]">{eyebrow}</p><h1 className="display mt-3 text-5xl">{title}</h1><p className="mt-3 max-w-xl font-sans text-sm text-[var(--muted)]">{description}</p></div><button className="bg-[var(--teal)] px-4 py-3 font-sans text-sm text-white">+ {action}</button></div><div className="mt-8 overflow-x-auto border-y border-[var(--line)]"><table className="w-full min-w-[640px] text-left font-sans text-sm"><thead className="border-b border-[var(--line)] text-[10px] uppercase tracking-wider text-[var(--muted)]"><tr>{columns.map(column => <th key={column.key} className="py-3">{column.label}</th>)}</tr></thead><tbody className="divide-y divide-[var(--line)]">{rows.map(row => <tr key={row.id} className="hover:bg-white/50">{columns.map(column => <td key={column.key} className="py-4">{column.render ? column.render(row) : row[column.key]}</td>)}</tr>)}</tbody></table></div>{rows.length === 0 && <p className="py-10 text-center font-sans text-sm text-[var(--muted)]">No records found.</p>}</div>; }
