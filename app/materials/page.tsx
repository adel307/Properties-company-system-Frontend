import { materialsApi } from '@/lib/api';
import EntityPage from '@/components/common/EntityPage';
export default async function MaterialsPage() { const response = await materialsApi.getAll(); const rows = Array.isArray(response) ? response : response?.data || []; return <EntityPage title="Materials" eyebrow="Material ledger" description="Know what is on site, what has been paid, and what still needs attention." rows={rows} action="Add material" columns={[{ key: 'name', label: 'Material' }, { key: 'quantity', label: 'Quantity' }, { key: 'status', label: 'Status' }, { key: 'remaining_amount', label: 'Remaining' }]} />; }
